# 指令审计与优化结果

审计日期：2026-09-14  
项目：or-ms-research-workflow  
基线：本地 HEAD `a2cac42`；开始审计时工作区干净。

## 依据与范围

本次读取了用户指定的 [GPT-6 Astra 官方模型指南](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra)，以其提示词建议为依据，检查指令冲突、执行中断、输出结构及验证范围。指南的建议按本项目需要采用，未机械复制通用提示词。

审计覆盖 SKILL.md、manifest.yaml、README.md、15 个主 references 及相关模板与样例引用。项目原先没有 AGENTS.md，本次新增项目维护入口。可见的上级 `C:/Users/86189/.codex/AGENTS.md` 为空，未修改。

这是指令优化，不是 API 或模型迁移。未发现需要替换的旧模型标识或 API 配置，也未修改模型设置、调用策略或账号配置。

## 主要发现与修复

| 原问题 | 修改与保留的边界 |
| --- | --- |
| SKILL.md、manifest 和 README 重复列举路由；宽泛关键词容易误触发多模块 | manifest 保留全部 14 个路由，按交付物选择；辅助模块改为条件加载，README 保留使用说明 |
| 任何技术术语的使用都触发完整术语流程 | 常规复用已定义术语只需保持一致；命名、冲突、来源或一致性审计才加载详细流程 |
| 新术语审批容易连带阻塞正文工作或重复索取已给出的确认 | 仍须作者批准后才能采用新名称；已有明确决定有效；等待期间使用中性表达并继续独立工作 |
| “不得发明定理和证明”没有区分伪造与新的研究推导 | 明确区分源文献结论、新推导、猜想、拟议实验和观察结果，禁止伪造证据或验证状态 |
| 正文未稳定时，引言流程仅允许返回提纲 | 按请求交付注明证据缺口的初稿或提纲；不虚构贡献、结果或章节结构 |
| 固定段落、四项贡献、逐段注释及输出清单被泛化为硬约束 | 保留叙事架构和注释模板，按任务适配；明确要求注释时仍保留作者的注释层次 |
| “定义先于使用”误伤引言对后文定理的正常前向引用 | 保留符号和概念先解释再实质使用；允许不依赖未解释符号的概述和可解析前向引用 |
| 每个定理都须给出管理建议及条件失效后的失败结论 | 只写有证据的含义；保证不适用不等于结论必然失败，技术引理可仅服务于后续结果 |
| 上界一律归入非正式解释；不够中心的结果建议降级 | 区分已证明的界与由其启发的非正式建议；结果地位依据证明和论文作用判断 |
| 局部“最终润色”触发全文审计与重复预读流程 | 按请求范围验证；完整投稿前审查仍保留章节检查、证据日志、全局一致性和未决事项 |
| 接受建议可被视为已实施修改 | Fixed / Done 只用于实际应用且检查过的修改；接受但未应用单独记录 |
| 修订经验总结可能自动改写持久技能 | 只有请求包含技能维护时才修改技能；项目术语和个别论文偏好不能自动升级为通用规则 |
| references 内的样例路径含重复根目录前缀 | 改为相对当前文件可解析的链接；历史样例内容保留 |

## 业务约束与授权

保留：证据真实性、主张与证明强度一致、重要假设和常数、阈值与边界情形、公平基准、符号和术语一致性、管理含义的证据支持，以及完整审计的可追溯状态。

新术语候选仍独立存放于决策说明；提出候选不等于批准采用。正式采用前保留作者决定，采用后记录定义与相关用法。作者已明确定义或批准的术语不再需要重复审批。

用户的明确任务和已有授权优先于本技能的流程及风格默认值，仍服从更高优先级指令和工具权限。审稿、论文修订或本次技能维护均不自动授权对外发信、投稿、发布、账号修改或其他技能修改。没有新增强制子代理流程。

## 精简幅度

行数及字符数按文本换行统一后计算，不等同于模型 token 数。

| 文件或范围 | 修改前 | 修改后 |
| --- | ---: | ---: |
| SKILL.md 行数 | 76 | 40 |
| SKILL.md 字符数 | 8,649 | 4,934 |
| manifest.yaml 行数 | 236 | 118 |
| README.md 行数 | 232 | 35 |
| 15 个主 references 字符数 | 60,430 | 51,139 |

SKILL.md 字符数减少约 43%，主 references 减少约 15%。另新增项目维护说明 AGENTS.md 和本审计报告；未为减少数字删除研究模块或样例。

## 验证结果

- **技能校验器通过**：运行 skill-creator 原有 `quick_validate.py`，返回 `Skill is valid!`。
- **结构检查通过**：技能 frontmatter、manifest 和 agents/openai.yaml 共 3 处 YAML 校验；manifest 与 UI YAML 另做重复键检查。
- **路由与资源检查通过**：原有 14 个路由全部保留，25 个 manifest 资源路径存在，覆盖 15 个主 references 与 10 个模板。
- **链接检查通过**：43 个本地 Markdown 链接均可解析，样例入口路径已修复。
- **边界文件检查通过**：4 个历史样例、agents/openai.yaml、LICENSE 和 .gitignore 保持原文；未改变技能默认调用策略。
- **差异检查通过**：`git diff --check` 无空白错误；修改文件统一为 UTF-8 和一致换行。

原校验器因环境缺少 PyYAML 首次未运行成功。依赖仅补充到临时目录，再在有权读取该目录的环境中运行原校验器和只读检查；未新增项目依赖或修改系统 Python 配置。

## 场景静态走查

以下为对最终指令与路由的人工静态走查，不是独立模型运行测试。

| 场景 | 最终指令的处理 |
| --- | --- |
| 仅润色一段含已有术语的正文 | 正文模块与受影响定义检查，不自动展开全文术语台账或审稿报告 |
| 未指定期刊，请求摘要 | 可交付常规无标签摘要，不因期刊问题停顿；投稿格式合规另需核验 |
| 正文尚未定稿，请求引言初稿 | 按证据交付初稿，明确缺口和暂定内容 |
| 请求两项贡献及无注释正文 | 遵循用户格式，不强制四项贡献或 LaTeX 注释 |
| 请求候选名称并继续建模 | 单独提供候选决策说明，建模使用中性描述继续进行 |
| 作者已经选择某个新名称并要求采用 | 记录已有决定，在授权范围内定义并统一用法，不重复确认 |
| 解释一个只证明充分条件的定理 | 说明保证范围，不凭条件失效推断结论失败 |
| 全文投稿前审查缺少附录 | 完成可检查部分并保留日志，附录证明标为未验证 |
| 作者接受改进建议，但尚未修改论文 | 记录接受状态，不写 Fixed / Done |
| 仅要求总结审稿意见 | 提供研究和修订建议，不自动改写可复用技能或发送消息 |

本次没有运行论文数值实验、重新证明样例定理、调用模型 API 或进行独立子代理评测。因此验证支持文件结构、资源可达性和指令逻辑的一致性，不构成运行时质量或性能提升的实证结论。
